// AI Lab extension: clearer Years 7–8 AI builder and data-quality experiment
(function(){
  const oldRenderBuild = renderBuild;
  const oldRenderTeach = renderTeach;
  defaultState.juniorDataLab={set:'good',question:'Can aluminium cans be recycled?',result:'',reflection:''};
  if(!state.juniorDataLab) state.juniorDataLab=clone(defaultState.juniorDataLab);

  const goodData=[
    'Aluminium cans can be recycled through many council recycling programs.',
    'Clean paper and cardboard are commonly accepted in recycling bins.',
    'Plastic recycling rules differ between councils, so check your local council guide.',
    'Food scraps should not be placed loose in a general recycling bin.'
  ];
  const badData=[
    'Everything with a recycling symbol can always go in any recycling bin.',
    'Dirty food containers are always fine in recycling.',
    'All plastic bags belong in the household recycling bin.',
    'If you are unsure, just guess — recycling rules are the same everywhere.'
  ];
  function juniorBuild(l,s){const total=6;
    if(s===0)return shell(l,s,total,`<div class="mission-card spotlight"><span class="pill good">SMALL AI, NOT CHATGPT</span><h3>What are we actually building?</h3><p>You are <strong>not training a giant AI model</strong>. In this lab you are designing a tiny classroom assistant that can only do the job, use the information and follow the rules you give it.</p><div class="concept-flow mini"><div><strong>1. JOB</strong><span>What should it help with?</span></div><b>→</b><div><strong>2. DATA</strong><span>What facts can it use?</span></div><b>→</b><div><strong>3. RULES</strong><span>How should it behave?</span></div><b>→</b><div><strong>4. TEST</strong><span>Does it give a useful answer?</span></div></div><div class="chunk-note"><strong>Think of it like:</strong> a new student helper. If you give the helper a clear job and reliable notes, it can help. If the notes are wrong, its answers may also be wrong.</div></div>`);
    if(s===1)return shell(l,s,total,`<div class="mission-card"><h3>Step 1 — Give it ONE small job</h3><p>A small AI works better when its job is clear. Choose one example first.</p><div class="choice-grid"><button class="preset" data-purpose="Answer simple recycling questions using only the supplied recycling facts.">♻️ Recycling Helper</button><button class="preset" data-purpose="Quiz Year 7 students using only the supplied science revision facts.">🧪 Science Quiz Coach</button><button class="preset" data-purpose="Answer questions about school routines using only the supplied school guide.">🏫 School Guide</button></div><div class="field"><label>Name your helper</label><input id="aiName" value="${esc(state.ai.name)}"></div><div class="field"><label>Its ONE job</label><textarea id="aiPurpose">${esc(state.ai.purpose)}</textarea></div><div class="chunk-note"><strong>Good:</strong> “Answer recycling questions from my facts.” <strong>Too broad:</strong> “Know everything and answer anything.”</div></div>`,{bind:()=>{aiName.oninput=e=>{state.ai.name=e.target.value;save()};aiPurpose.oninput=e=>{state.ai.purpose=e.target.value;save()};document.querySelectorAll('.preset').forEach(b=>b.onclick=()=>{state.ai.purpose=b.dataset.purpose;save();renderLesson()})}});
    if(s===2)return shell(l,s,total,`<div class="mission-card"><h3>Step 2 — Who will use it?</h3><p>Imagine you are explaining the same idea to a Year 3 student and a teacher. You would probably use different words. Your AI needs to know its audience too.</p><div class="choice-row"><button class="jun-aud" data-a="Years 7–8">Students like me</button><button class="jun-aud" data-a="Younger students">Younger students</button><button class="jun-aud" data-a="Teachers">Teachers</button></div><div class="choice-row"><button class="personality" data-p="Friendly and concise">Friendly + short</button><button class="personality" data-p="Calm and encouraging">Calm + encouraging</button><button class="personality" data-p="Professional and factual">Factual</button></div><div class="feedback">Audience: ${esc(state.ai.audience)} • Style: ${esc(state.ai.personality)}</div></div>`,{bind:()=>{document.querySelectorAll('.jun-aud').forEach(b=>b.onclick=()=>{state.ai.audience=b.dataset.a;save();renderLesson()});document.querySelectorAll('.personality').forEach(b=>b.onclick=()=>{state.ai.personality=b.dataset.p;save();renderLesson()})}});
    if(s===3)return shell(l,s,total,`<div class="mission-card"><h3>Step 3 — Give it rules</h3><p>Rules tell your helper what to do when it does not know an answer.</p><div class="toggle-list">${['Stay on topic','Use only the supplied facts','Ask when information is missing','Do not invent facts','Say when unsure','Do not request personal information'].map(r=>`<label class="rule-toggle"><input type="checkbox" value="${r}" ${state.ai.rules.includes(r)?'checked':''}>${r}</label>`).join('')}</div><div class="chunk-note"><strong>Example:</strong> If someone asks your Recycling Helper “Who won the football?”, the best response is “That is outside my job,” not a guess.</div></div>`,{bind:()=>document.querySelectorAll('.rule-toggle input').forEach(i=>i.onchange=()=>{state.ai.rules=[...document.querySelectorAll('.rule-toggle input:checked')].map(x=>x.value);save()})});
    if(s===4)return shell(l,s,total,`<div class="mission-card"><h3>Step 4 — Questions matter too</h3><p>Your AI needs a question it can actually answer from its job and data.</p><div class="example-grid"><div class="quick-q"><strong>✅ Good question</strong><p>“Can an aluminium can go in recycling?”</p><small>Specific and matches the recycling data.</small></div><div class="quick-q"><strong>⚠️ Poor question</strong><p>“Tell me everything about rubbish.”</p><small>Too broad. The small helper does not have enough information.</small></div><div class="quick-q"><strong>❌ Outside its job</strong><p>“Who is the best football player?”</p><small>The helper should say it does not know.</small></div></div><div class="field"><label>Write one question your helper should be able to answer</label><textarea id="juniorQuestion">${esc(state.juniorDataLab.question)}</textarea></div></div>`,{bind:()=>juniorQuestion.oninput=e=>{state.juniorDataLab.question=e.target.value;save()}});
    return shell(l,s,total,`<div class="mission-card"><h3>Your small AI blueprint</h3><div class="ai-profile"><div class="bot-face">AI</div><div><h3>${esc(state.ai.name)}</h3><p><strong>Job:</strong> ${esc(state.ai.purpose)}</p><p><strong>Audience:</strong> ${esc(state.ai.audience)}<br><strong>Style:</strong> ${esc(state.ai.personality)}</p></div></div><p><strong>Rules:</strong> ${state.ai.rules.map(esc).join(' • ')}</p><div class="responsible-code"><strong>Important:</strong><p>You have designed the instructions and knowledge for a small classroom AI simulation. You have not trained a large model like ChatGPT. Next you will see why the quality of the information you give it matters.</p></div></div>`);
  }
  function juniorTeach(l,s){const total=5,d=state.juniorDataLab,data=d.set==='good'?goodData:badData;
    if(s===0)return shell(l,s,total,`<div class="mission-card spotlight"><h3>🧠 An AI can only work with what it has</h3><p>Imagine two students sit the same quiz. One studies from accurate teacher notes. The other studies from a page full of mistakes. Who is more likely to answer correctly?</p><div class="concept-flow mini"><div><strong>DATA IN</strong><span>Facts and examples</span></div><b>→</b><div><strong>AI USES IT</strong><span>Finds useful patterns/context</span></div><b>→</b><div><strong>ANSWER OUT</strong><span>Quality can change</span></div></div><div class="chunk-note"><strong>Key idea:</strong> Better data does not guarantee a perfect answer, but poor or misleading data can make reliable answers much harder.</div></div>`);
    if(s===1)return shell(l,s,total,`<div class="mission-card"><h3>Compare two data packs</h3><div class="choice-row"><button class="data-set ${d.set==='good'?'selected':''}" data-set="good">✅ Good data</button><button class="data-set ${d.set==='bad'?'selected':''}" data-set="bad">⚠️ Bad data</button></div><div class="knowledge-list">${data.map((x,i)=>`<div class="knowledge-card"><strong>Fact ${i+1}</strong><p>${esc(x)}</p></div>`).join('')}</div><p>${d.set==='good'?'<strong>Notice:</strong> these statements are careful, specific and admit when local rules may differ.':'<strong>Notice:</strong> these statements use words like “always”, encourage guessing, or make broad claims without checking.'}</p></div>`,{bind:()=>document.querySelectorAll('.data-set').forEach(b=>b.onclick=()=>{d.set=b.dataset.set;save();renderLesson()})});
    if(s===2)return shell(l,s,total,`<div class="mission-card"><h3>Ask the SAME question</h3><p>To make this a fair test, keep the question the same and only change the data.</p><div class="field"><label>Question</label><textarea id="dataQuestion">${esc(d.question)}</textarea></div><button id="runDataTest" class="primary">Run classroom AI simulation</button><div id="dataResult" class="feedback ${d.result?'good':''}">${d.result?esc(d.result):'Run the test to see the answer.'}</div></div>`,{bind:()=>{dataQuestion.oninput=e=>{d.question=e.target.value;save()};runDataTest.onclick=()=>{const q=d.question.toLowerCase();if(d.set==='good'){d.result=q.includes('aluminium')||q.includes('can')?'Based on the supplied data: Yes, aluminium cans can be recycled through many council recycling programs. Check your local council rules if unsure.':'I should only answer from the supplied recycling facts. I may need more information.'}else{d.result='Based on the supplied data: Everything with a recycling symbol can always go in any recycling bin, so yes. (This sounds confident, but the supplied data is unreliable.)'}save();renderLesson()}}});
    if(s===3)return shell(l,s,total,`<div class="mission-card"><h3>Now change ONLY the data</h3><p>Your question stays the same. Switch between the two packs and run the simulation again.</p><div class="choice-row"><button class="data-set" data-set="good">Use GOOD data</button><button class="data-set" data-set="bad">Use BAD data</button></div><div class="feedback"><strong>Current pack:</strong> ${d.set==='good'?'GOOD DATA':'BAD DATA'}</div><div class="chunk-note"><strong>This is an experiment:</strong> Question = same. Small AI rules = same. Data = changed. If the answer changes, you have evidence that data quality matters.</div></div>`,{bind:()=>document.querySelectorAll('.data-set').forEach(b=>b.onclick=()=>{d.set=b.dataset.set;d.result='';save();setStep(l.id,2)})});
    return shell(l,s,total,`<div class="mission-card"><h3>What did you discover?</h3><div class="field"><label>Complete this sentence: The data given to an AI matters because...</label><textarea id="dataReflection">${esc(d.reflection)}</textarea></div><div class="quick-q"><strong>Remember</strong><p>Real AI systems are much more complicated than this classroom simulation. But the principle is real: the information used to build, train, ground or prompt an AI can affect its outputs. Humans still need to check important answers.</p></div></div>`,{bind:()=>dataReflection.oninput=e=>{d.reflection=e.target.value;save()}});
  }
  renderBuild=function(l,s){return isJunior()?juniorBuild(l,s):oldRenderBuild(l,s)};
  renderTeach=function(l,s){return isJunior()?juniorTeach(l,s):oldRenderTeach(l,s)};
  save();renderLesson();
})();